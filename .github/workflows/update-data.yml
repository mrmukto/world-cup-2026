name: Update World Cup data

on:
  schedule:
    # Roughly every 15 min, but offset off :00/:15/:30/:45 — GitHub delays/skips cron
    # most around the top of the hour (its busiest window). Odd minutes are more reliable.
    - cron: "7,22,37,52 * * * *"
  workflow_dispatch: {}   # lets you trigger a refresh manually from the Actions tab

permissions:
  contents: write          # needed so the job can commit the refreshed JSON

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Fetch latest data
        run: python scripts/update_data.py   # worldcup26.ir — free, no API key needed

      - name: Commit if changed
        run: |
          git config user.name  "wc-bot"
          git config user.email "actions@github.com"
          git add data/tournament.json
          if git diff --cached --quiet; then
            echo "No changes."
          else
            git commit -m "data: refresh $(date -u +'%Y-%m-%dT%H:%MZ')"
            git push
          fi
